<%- include("partials/header") -%>

<h1>Home</h1>
<%= homeContent %>

<%    posts.forEach(post => {   %>
    <a href="id/<%= post.title %>"><h2><%= post.title %></h2></a>
    <!-- TO SLICE STRING -->
    <p>
        <%= post.body.substring(0, 100) + "..." %>
        <a href="id/<%= post.title %>">Read More</a>
    </p>
<%    });   %>

<%- include("partials/footer") -%>


